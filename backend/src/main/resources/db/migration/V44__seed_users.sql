-- Seed 7 roles for SGI Procurement system
-- NOTE: BCrypt hashes must be generated at runtime.
-- This SQL uses placeholder hashes — the DataSeeder component will
-- ensure accounts exist with correct BCrypt encoding on startup.
-- If running without the application seeder, generate hashes manually
-- at https://bcrypt-generator.com (cost=10) and replace below.

-- Skip if already seeded (users will be created by DataSeeder if missing)
-- See com.sgiprocurement.config.DataSeeder
